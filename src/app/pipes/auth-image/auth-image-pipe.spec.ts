import { AuthImagePipe } from './auth-image-pipe';

describe('AuthImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AuthImagePipe();
    expect(pipe).toBeTruthy();
  });
});
